---
layout: home

hero:
  name: Continuum
  text: Реактивный UI без ре-рендеров
  tagline: Компонент выполняется один раз. Состояние — реактивное значение, которое кладётся прямо в JSX; фреймворк сам держит DOM в синхроне, по одному текстовому узлу за раз.
  actions:
    - theme: brand
      text: Начать
      link: /ru/overview
    - theme: alt
      text: Пришли из React?
      link: /ru/from-react
    - theme: alt
      text: GitHub
      link: https://github.com/denislibs/continuum

features:
  - title: Нет ре-рендеров
    details: Функция компонента выполняется ровно один раз. Состояние — значение, которое кладётся в JSX; обновляется только зависящий от него узел или атрибут — без хуков, массивов зависимостей и виртуального DOM.
  - title: Изменение — это данные. Этого нет ни у кого
    details: Действия пользователя — поток, который сворачивается в состояние. Один поток действий — и undo, персист, синхронизация вкладок и поиск без гонок становятся одной дополнительной свёрткой каждый, а не библиотекой каждый.
  - title: Гарантированно без багов обновления
    details: Все обновления атомарны — производные значения не могут увидеть полуобновлённое состояние. Это гарантия семантики классического FRP, а не дисциплина.
  - title: Достаточно маленький, чтобы прочитать
    details: "Целое приложение — фреймворк, состояние и ваш код — собирается в 3,7 кБ gzip JavaScript; одни React + ReactDOM в ~12 раз больше. Жёсткие бюджеты размера проверяются в CI."
---

## Попробовать

```bash
npm create continuum-js@latest my-app
```

```tsx
import { newBehavior } from "@continuum-js/frp";
import { mount } from "@continuum-js/dom";

function Counter() {
  const [count, setCount] = newBehavior(0);
  return (
    <button onClick={() => setCount(count.sample() + 1)}>count: {count}</button>
  );
}

mount(document.getElementById("app")!, () => <Counter />);
```

Если вы знаете `useState`, вы уже знаете и это — с одной разницей:
`Counter` больше никогда не выполнится. `{count}` привязывает текстовый узел
к значению; клик патчит ровно этот узел.

## А дальше — то, чего нет у других

Всё, что выше, обещает и Solid. Вот часть, которой у него нет: **само
изменение — это значение.** Проведите каждое изменение через один поток
действий и сверните его — и фичи, на которые обычно берут по библиотеке,
становятся одной строкой каждая:

```tsx
const [actions, dispatch] = newStream<Action>();

const todos = actions.accum(loadPersisted("todos", []), reduce); // состояние
const history = actions.accum(emptyHistory, undoReduce); //       + undo
onCleanup(persist("todos", todos)); //                            + персист
// событие `storage`, диспатчащее в тот же редьюсер:              + синхронизация вкладок
```

Редьюсер при этом не меняется ни на букву. [Книга
паттернов](/ru/guides/patterns) проходит всё это — undo, оптимистичные
обновления, поиск без гонок — по несколько строк поверх одного потока.
