// Russian translations for the tutorial. Code (starter/solution/check) is
// shared and universal; only the human-readable strings — track title/blurb,
// step title/task/hint — are translated here, plus the UI labels. The
// Tutorial component looks these up when lang="ru", falling back to English.

export interface StepTr {
  title: string;
  task: string;
  hint?: string;
}
export interface TrackTr {
  title: string;
  blurb: string;
  steps: Record<string, StepTr>;
}

export const ui = {
  run: "Запустить",
  check: "Проверить",
  checking: "Проверяю…",
  preview: "Превью",
  next: "Дальше →",
  hint: "Подсказка",
  showSolution: "Показать решение",
  passed: "Готово! 🎉",
  notYet: "Пока нет",
  mastered: "освоено!",
  masteredNote: "Вы прошли все шаги. Отличная работа.",
  shareBadge: "Поделиться значком",
  copied: "Скопировано ✓",
  runThenTab: "// Запустите, затем откройте эту вкладку",
};

export const tracks: Record<string, TrackTr> = {
  basics: {
    title: "Основы",
    blurb: "Состояние, производные значения и рендеринг — базовый цикл.",
    steps: {
      state: {
        title: "Кусочек состояния",
        task: `Всё реактивное в Continuum начинается с **состояния** — значения,
которое меняется во времени. Создайте его через \`state(initial)\` и положите
прямо в JSX; Continuum привяжет к нему текстовый узел.

**Цель:** отрендерить абзац с числом \`0\`.`,
        hint: "Положите состояние между тегами: <p>{count}</p>.",
      },
      set: {
        title: "Изменить",
        task: `Чтобы изменить состояние относительно прошлого значения, используйте
\`.update(prev => next)\` — оно даёт текущее значение и берёт новое. (Есть и
\`.set(value)\` — заменить свежим значением целиком.) Ничего не ре-рендерится —
патчится только привязанный текстовый узел.

**Цель:** клик по кнопке увеличивает счётчик.`,
        hint: "count.update((n) => n + 1)",
      },
      on: {
        title: "Свернуть события в состояние",
        task: `Ручное чтение и запись работают, но идиоматичнее — свернуть **поток**
событий в состояние. \`stream()\` создаёт источник событий; \`.on\` переводит
состояние на каждом событии.

**Цель:** пересоберите счётчик через \`stream\` + \`.on\` вместо \`.set\`.`,
        hint: "const count = state(0).on(clicks, (n) => n + 1);",
      },
      map: {
        title: "Производные значения",
        task: `Производное значение — это просто другое состояние, вычисленное из
существующего через \`.map\`. Оно обновляется само, без лишнего учёта.

**Цель:** покажите и счётчик, и его удвоение, напр. \`1 / 2\`.`,
        hint: "count.map((n) => n * 2)",
      },
      combine: {
        title: "Объединить два состояния",
        task: `Чтобы получить значение из **нескольких** состояний, используйте
\`combine\`. Оно пересчитывается при изменении любого входа.

**Цель:** отрендерьте сумму \`a\` (3) и \`b\` (4) — ожидается \`7\`.`,
        hint: "combine(a, b, (x, y) => x + y)",
      },
      input: {
        title: "Управляемый инпут",
        task: `\`bindInput\` связывает \`<input>\` со строковым состоянием: раскройте
его в элемент, и состояние отслеживает то, что вводит пользователь.

**Цель:** отобразите введённый текст в абзаце под инпутом.`,
        hint: "Подставьте состояние: <p>Hello {name}</p>.",
      },
      show: {
        title: "Условный рендеринг",
        task: `\`<Show>\` монтирует детей только когда состояние истинно, иначе —
опциональный \`fallback\`. Поддерево строится один раз и сохраняется — не
пересоздаётся на каждое изменение.

**Цель:** показывать \`"Even"\` при чётном счётчике и \`"Odd"\` при нечётном.`,
        hint: "Раскомментируйте блок <Show>: when — isEven, fallback рендерит Odd.",
      },
      list: {
        title: "Ключевой список",
        task: `\`<Each>\` рендерит список из состояния-массива, ключуя по \`by\`. При
изменении массива он перемещает и патчит существующие строки, а не пересобирает.

**Цель:** отрендерьте по одному \`<li>\` на фрукт.`,
        hint: "Раскомментируйте блок <Each>; ключуйте по фрукту: by={(f) => f}.",
      },
    },
  },
  streams: {
    title: "Потоки",
    blurb: "События как значения: свёртка, hold, слияние, сэмплирование.",
    steps: {
      accum: {
        title: "События — это поток",
        task: `**Поток** — это значение, которое происходит: клик, сообщение, ответ.
\`stream()\` создаёт его; \`.fire\` пушит происшествие. Сверните поток через
\`.accum(init, (event, acc) => next)\` — и получите **состояние**: текущий
результат всего, что случилось.

**Цель:** считать клики, сворачивая поток кликов (без \`.set\`).`,
        hint: "clicks.accum(0, (_event, n) => n + 1) — возвращает State.",
      },
      hold: {
        title: "Держать последнее",
        task: `\`.hold(initial)\` превращает поток значений в **состояние**, которое
всегда хранит последнее. Идеально, когда важно только «что сейчас».

**Цель:** показать букву последней нажатой кнопки (старт с \`—\`).`,
        hint: "const current = picks.hold('—');",
      },
      filter: {
        title: "Фильтровать поток",
        task: `У потоков есть ожидаемые операторы: \`.map\` преобразует каждое
происшествие, \`.filter\` отбрасывает неподходящие — до того, как они дойдут до
состояния.

**Цель:** считать только **чётные** тики. Клик выпускает 1, 2, 3, … по очереди.`,
        hint: "ticks.filter((n) => n % 2 === 0)",
      },
      or: {
        title: "Слить два источника",
        task: `\`.or\` сливает два потока одного типа в один. Сначала отобразите
каждый источник в его смысл, потом сверните слитый поток один раз.

**Цель:** один total, питаемый кнопками **+1** и **+10**.`,
        hint: "plus1.mapTo(1).or(plus10.mapTo(10))",
      },
      at: {
        title: "Сэмплировать состояние в событии",
        task: `\`.at(stream)\` читает значение состояния **в момент**, когда поток
срабатывает — классическая форма «submit»: захватить черновик по клику, а не на
каждое нажатие клавиши.

**Цель:** показать текст, каким он был при нажатии **submit**.`,
        hint: "draft.at(submit) — поток значения черновика на каждый submit; сделайте hold.",
      },
      on: {
        title: "Много источников, одно состояние",
        task: `\`state(init).on(stream, reducer)\` сворачивает поток в состояние, и
можно цепочкой \`.on\` для нескольких источников — каждый шаг это
\`(state, event) => state\`.

**Цель:** счётчик с **+** и **−**.`,
        hint: "Добавьте ещё: .on(dec, (n) => n - 1)",
      },
    },
  },
  dom: {
    title: "DOM и компоненты",
    blurb: "Компоненты, привязки атрибутов, Show, Each, Dynamic, контекст.",
    steps: {
      components: {
        title: "Композиция компонентов",
        task: `Компонент — функция, возвращающая JSX. Выполняется **один раз**;
вкладывайте компоненты как любой элемент, передавая данные через props.

**Цель:** вынесите компонент \`Greeting\` и отрендерьте для Ada и Bob.`,
        hint: 'Используйте как тег: <Greeting name="Ada" />',
      },
      class: {
        title: "Привязать атрибут",
        task: `Любой атрибут принимает **производное состояние**. Положите \`State\`
в \`class={…}\` — и при изменении обновится только этот атрибут, без ре-рендера.

**Цель:** класс кнопки должен быть \`"on"\` когда active, иначе \`"off"\`.`,
        hint: "class={active.map((a) => (a ? 'on' : 'off'))}",
      },
      show: {
        title: "Show — и передача значения",
        task: `\`<Show when={s}>\` монтирует детей только когда \`s\` истинно, с
опциональным \`fallback\`. Дети получают **не-null значение** — его можно
использовать сразу.

**Цель:** поприветствовать вошедшего пользователя по имени; иначе «logged out».`,
        hint: "Ребёнок получает значение: {(u) => <p>hello {u.name}</p>}",
      },
      each: {
        title: "Растущий список",
        task: `\`<Each>\` держит ключевой список в синхроне с состоянием-массивом.
Добавьте элемент в массив — и он вставит ровно одну строку, не трогая остальные.

**Цель:** кнопка **add** должна добавлять элемент.`,
        hint: "items.update((list) => [...list, 'item ' + ++n])",
      },
      dynamic: {
        title: "Переключить поддерево",
        task: `\`<Dynamic value={s}>\` пересобирает детей при каждом изменении \`s\` —
когда от режима или роута зависит целый регион, а не одно значение.

**Цель:** показывать «Panel A» или «Panel B» в зависимости от вкладки.`,
        hint: "{(t) => (t === 'a' ? <p>Panel A</p> : <p>Panel B</p>)}",
      },
      context: {
        title: "Контекст",
        task: `Контекст передаёт значение вниз по дереву без прокидывания пропсов.
Создайте через \`createContext(default)\`, задайте через \`provide(ctx, value)\`,
читайте в любом потомке через \`use(ctx)\`.

**Цель:** передать тему \`"dark"\`, чтобы ребёнок её прочитал.`,
        hint: "provide(Theme, 'dark'); перед возвратом <Child />",
      },
    },
  },
  patterns: {
    title: "Паттерны",
    blurb: "Реальные фичи в несколько строк: фильтр, суммы, undo, форма todo.",
    steps: {
      filter: {
        title: "Фильтрованный список",
        task: `Производный список — это просто \`.map\` над состоянием-массивом.
Отфильтрованный результат — тоже состояние, поэтому \`<Each>\` держит DOM в
синхроне по мере ввода.

**Цель:** показывать только фрукты, содержащие введённое.`,
        hint: "const shown = query.map((q) => ALL.filter((x) => x.includes(q)));",
      },
      total: {
        title: "Производная сумма",
        task: `Суммы никогда не рассинхронизируются, потому что не хранятся — они
**вычисляются** из источника через \`.map\`. Меняется источник — сумма следует.

**Цель:** показать сумму цен; **add $5** должно её обновлять.`,
        hint: "prices.map((xs) => xs.reduce((a, b) => a + b, 0))",
      },
      undo: {
        title: "Undo в несколько строк",
        task: `Undo — не библиотека, а другая **свёртка**. Держите историю прошлых
значений рядом с текущим; \`+1\` пушит текущее в прошлое, \`undo\` возвращает его.
Редьюсеры — это и есть вся фича.

**Цель:** сделайте, чтобы **undo** восстанавливал предыдущий счёт.`,
        hint: "Добавьте .on(undo, h => h.past.length ? { past: h.past.slice(0,-1), present: h.past[h.past.length-1] } : h)",
      },
      todo: {
        title: "Форма todo",
        task: `Всё вместе: состояние черновика, поток submit и список, свёрнутый из
отправок. \`.at(submit)\` захватывает черновик, \`.filter\` отбрасывает пустые,
\`.accum\` строит список — а \`<Each>\` его рендерит.

**Цель:** ввод текста и нажатие **add** должны добавлять todo.`,
        hint: "todos = draft.at(submit).filter(t => t.trim()).accum([], (text, list) => [...list, { id: ++id, text }])",
      },
    },
  },
};
